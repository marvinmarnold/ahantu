<ul class="thumbnails">
  <% room.photos.each_slice(4 )do |photo_slice| %>
    <div class="row-fluid">
      <% photo_slice.each do |photo| %>
      <li class="span3"><%= image_tag photo.photo_url(:banner) %></li><% end %>
    </div>
  <% end %>
</ul>