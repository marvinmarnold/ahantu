<%= simple_form_for(@contact_form) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :subject %>
    <%= f.input :from %>
    <%= f.input :body %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
