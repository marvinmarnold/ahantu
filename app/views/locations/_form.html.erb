<%= simple_form_for(@location) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :ancestry %>
    <%= f.input :name %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
